<body>
         <h1>List of Books</h1>
         <table>
                          <tr>
                              <th>Réf</th>
                              <th>Title</th>
                              <th>Published</th>
                              <th>Publication Date</th>
                              <th>Category</th>
                              <th>Author</th>

                          </tr>
          {% for book in books %}
                             <tr>
                              <td>{{ book.ref }}</td>
                              <td>{{ book.title | upper }}</td>
                              <td>{{ book.enabled }}</td>
                              <td>{{ book.publicationDate | date }}</td>
                              <td>{{ book.category }}</td>
                              <td>{{ book.author }}</td>
                              </tr>
         {% endfor %}
                </table>