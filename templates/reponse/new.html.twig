{% extends 'base.html.twig' %}

{% block title %}New Reponse{% endblock %}

{% block body %}
<center>

    <div class="container mt-3">
        <h1 class="text-white">Create new Reponse</h1>

        {{ form_start(form, {'attr': {'class': 'text-white'}}) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-primary">Save</button>
        {{ form_end(form) }}

        <a href="{{ path('app_reponse_index') }}" class="text-white">Back to list</a>
    </div>
    </center>
{% endblock %}
